# Configuración de la base de datos MongoDB
MONGODB_URI=mongodb+srv://<usuario>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority

# Configuración del servidor
PORT=5000
NODE_ENV=development

# Orígenes permitidos por CORS (separados por comas). Si se deja vacío, se permite todo (solo dev).
# Ejemplo: FRONTEND_URL=http://localhost:5173,https://miweb.com
FRONTEND_URL=

# Configuración de Cloudinary para subida de archivos
CLOUDINARY_CLOUD_NAME=tu_cloud_name
CLOUDINARY_API_KEY=tu_api_key
CLOUDINARY_API_SECRET=tu_api_secret

# Configuración de JWT para autenticación
JWT_SECRET=tu_jwt_secret_super_seguro
JWT_EXPIRE=1h

# Resend para envío de correos (recuperación de contraseña)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
# Remitente. Para pruebas usa onboarding@resend.dev (solo envía a tu propio correo).
# Para producción, verifica tu dominio en Resend y usa algo como: La Aterciopelada <no-reply@tudominio.com>
RESEND_FROM=La Aterciopelada <onboarding@resend.dev>
